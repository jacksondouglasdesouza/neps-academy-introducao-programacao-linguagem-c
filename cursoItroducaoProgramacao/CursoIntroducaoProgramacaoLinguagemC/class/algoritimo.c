#include <stdio.h>
#include <stdlib.h>

int main(){

    /*Algoritmo para calcular a média de um aluno

    Anote a primeira nota; 

    Anote a segunda nota;

    Calcule a média aritmética das notas;

    Se a média for maior ou igual a 7, o alunos está aprovado;

    Se a média for menor que 7, mas maior ou igual a 4, o alunos está de avaliação final;

    Se a média for menor que 4, o alunos está reprovado;
    
    */

    //--

    /*Algoritmo para calcular a média de um aluno usando pseudocódigo, logo logo você verá que essa representação é bem 
    mais parecida com uma linguagem de programação convencional.

    Variáveis: Nota1, Nota2, Media
    Leia(Nota1, Nota2)
    Media = (Nota1+Nota2)/2
    Se (Media >= 7) Imprima "Aluno está aprovado"
    Se (Media < 7 e Media >= 4) Imprima "Aluno está de avaliação"
    Se (Media <= 4) Imprima "Aluno está reprovado"
    
    */

    // fluxograma
    /*
    Início
    Ler Nota1
    Ler Nota2
    Media = (Nota1 + Nota2) / 2
    Se (Media >= 7) Então
        Imprimir "Aluno está aprovado"
    Senão Se (Media < 7 e Media >= 4) Então
        Imprimir "Aluno está de avaliação"
    Senão
        Imprimir "Aluno está reprovado"
    Fim
    */

    // Image, do fluxograma, pode ser encontrada no repositório do curso.
    // .\CursoIntroducaoProgramacaoLinguagemC\class\img\fluxograma.png

    
}